<UserControl
    x:Class="Commander.LanguagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    FontFamily="Microsoft YaHei UI">

    <UserControl.Resources>
        <Style x:Key="LangButtonStyle" TargetType="ToggleButton">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="Microsoft YaHei UI" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bg"
                                CornerRadius="6"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                Background="{DynamicResource ControlFillColorDefaultBrush}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  TextElement.FontSize="{TemplateBinding FontSize}"
                                                  TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                <TextBlock x:Name="Check"
                                           Text="✓"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           FontSize="16"
                                           Foreground="{DynamicResource AccentFillColorDefaultBrush}"
                                           Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1A0078D4" />
                                <Setter TargetName="Bg" Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel>

        <!-- Header card -->
        <Border Style="{StaticResource GlassCard}" Margin="0,16,0,12">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="🌐"
                           FontSize="36"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,8" />
                <TextBlock x:Name="LblTitle"
                           Text="选择语言"
                           FontSize="18"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock x:Name="LblDesc"
                           Text="切换应用界面语言"
                           FontSize="12"
                           Margin="0,4,0,4"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Border>

        <!-- Buttons card -->
        <Border Style="{StaticResource GlassCard}">
            <StackPanel>
                <ToggleButton x:Name="BtnZh"
                              Content="中文"
                              Height="44"
                              Margin="0,0,0,8"
                              Style="{StaticResource LangButtonStyle}"
                              Click="BtnZh_Click" />
                <ToggleButton x:Name="BtnEn"
                              Content="English"
                              Height="44"
                              Style="{StaticResource LangButtonStyle}"
                              Click="BtnEn_Click" />
            </StackPanel>
        </Border>

    </StackPanel>
</UserControl>
